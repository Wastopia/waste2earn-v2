{
  "name": "@icpswap/tokens",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --pretty --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf build",
    "dev": "tsc --watch",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@icpswap/types": "../types",
    "@icpswap/swap-sdk": "../swap-sdk",
    "@icpswap/constants": "../constants"
  }
}